import { Page } from '@/components/page';
import { Welcome } from '@/components/welcome';

const Home = () => {
    return (
        <Page>
            <Welcome />
        </Page>
    );
};

export default Home;
